PROJECT BACKLOG (THINGS LEFT TO DO...)
-----------------------------------------
> implement cd   ( )  
    > checks if the file being changed to exists in the current directory ( )
    > checks if the file being changed to is a directory ( )
    > changes the current byte address to that location ( )
        > grabs the byte address of the directory entry for that directory ( )
        > converts the first cluster number of that directory into a byte address ( )
    > implement moving to "." and ".."
> implement ls   ( )
    > prints all file names in the current directory ( )
    > skips over prints deleted files and volume labels ( )
    > formats all dates to be printed ( )
    > print "." and ".." if we are not in the root directory ( )
> implement stat ( )
    > prints out file name        ( )
    > prints out file attributes  ( )
    > prints out file size        ( )
    > checks if the inputted file name exists in current directory ( )
    > prints an error message if the file doesn't exist ( )
> implement read ( )
    > checks to see if the file exists in the current directory ( )
    > prints an error if the file doesn't exist ( )
    > prints an error if the file being read is a directory ( )
    > prints a range of bytes to the user ( ) 
> implement info ( )
    > prints bytes per sector ( )
    > prints sectors per cluster ( )
    > prints reserved sector count ( )
    > prints number of FATs ( )
    > prints size of FATs ( )
> implement volume ( )
    > finds the volume label for the file system image ( )
    > if there is not volume label, it prints an error ( )
> implement checkFileExists() ( )
    > loops through all bytes, sectors, and clusters, associated with the current directory ( )
    > compares inputted file name with all valid directory entries ( )
    > when comparing, it skips over... ( )
        > volume labels ( )
        > empty directory entires ( )
        > long name entries ( )
    > halts at the end of the directory ( )
> update README
    > add descriptions for new files in the README ( )
